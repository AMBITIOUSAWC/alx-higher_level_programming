#ifndef MAIN_H
#define MAIN_H

/* Function prototype for finding a peak in a list of unsorted integers */
int find_peak(int *array, size_t size);
def find_peak(list_of_integers):

#endif /* MAIN_H */
